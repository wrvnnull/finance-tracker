/*
 * config.js
 * Isi APPS_SCRIPT_URL & TOKEN SETELAH kamu deploy Google Apps Script (lihat README).
 * Kalau dibiarin kosong, app jalan di MODE DEMO (data disimpan di localStorage browser).
 */
window.APP_CONFIG = {
  // URL Web App dari Deploy > Manage deployments di Google Apps Script
  APPS_SCRIPT_URL: '',

  // Token rahasia yang SAMA dengan variabel TOKEN di dalam Code.gs
  TOKEN: '',

  // Mata uang yang ditampilkan
  CURRENCY: 'Rp'
};
